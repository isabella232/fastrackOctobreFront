import React, { useState } from 'react';


const SubSkills = ({ techno }) => {
  const [active, setActive] = useState(true);

  console.log('Techno =>', techno);
  // récupérer
  return (
    <>
    </>
  );
};

export default SubSkills;

// Si categoryList[0].subcategory.name === filter(key(SkillsList))
// AFFICHE
// Il faut faire un filtre sur les clefs en fonction de la valeur de categoryList[0].subcategory.name

// if e.value==front va chercher les categories dans front et affiche lès.
