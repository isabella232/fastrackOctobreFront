import styled from '@emotion/styled';

const Navbar = styled.div({
  width: '80px',
  height: '100vh',
  background: '#29AAE2',
  display: 'flex',
  flexDirection: 'column',
  padding: '10rem 0',
  position: 'fixed',
});

export default Navbar;
