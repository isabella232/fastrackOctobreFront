import styled from '@emotion/styled';

const Container = styled.div`
  display: flex;
  width: calc(100% - 80px);
  height: 95vh;
  margin-left: 80px;
  padding: 7rem;
  position: relative;
  flex-wrap: wrap;
`;

export default Container;
